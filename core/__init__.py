"""
core/ — ядро програми. Копіюється між проєктами майже без змін.

Обов'язкові модулі: logger, crash_reporter, cleanup, config_manager, build_info,
_build_dist, updater, events, instance_lock. Опційні (crypto, health,
state_manager, env_loader) лежать в optional_modules/core/ — підключати за потреби.
"""
