"""
_build_secrets.py.example — шаблон файлу секретів.

Скопіювати як core/_build_secrets.py і заповнити GITHUB_TOKEN. Сам файл
core/_build_secrets.py — у .gitignore: навіть якщо забудеш прибрати токен перед комітом,
він фізично не потрапить у git-історію. Цей .example-файл, навпаки, версіюється навмисно —
без нього ніхто не здогадається, що файл секретів взагалі потрібен.

Без core/_build_secrets.py build_info.py падає на дефолт GITHUB_TOKEN = "" (dev-режим,
оновлення локально не тестуються) — програма НЕ падає через відсутність файлу.
"""
GITHUB_TOKEN: str = ""   # ← fine-grained PAT, лише Contents: Read-only, ОДИН репозиторій
