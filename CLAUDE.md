# CLAUDE.md — PhonesStatus

> Інструкція для Claude Code (Desktop і VS Code). Читається автоматично при відкритті проєкту.
> Проєкт створено на основі універсального Python Desktop App Template.

---

## Проєкт

- **Назва:** PhonesStatus
- **Версія:** 1.3.0
- **Опис:** порівнює статуси внутрішніх телефонів між таблицею Excel і поточним станом
  FreePBX (вставленими списками Online/Offline) і показує, які номери підключились,
  а які відключились. З 1.3.0 додатково дістає поточні IP телефонів із DHCP-lease
  роутера MikroTik і записує їх у Google-таблицю (окремий потік, не звірка).
- **Стек:** Python 3.13 + CustomTkinter + PyInstaller (Windows-only)

Уся ідентичність — у `core/build_info.py` (`APP_NAME`, `APP_VERSION`, `GITHUB_REPO`,
`GITHUB_PRIVATE`, `TEMP_PREFIX`, `TEMPLATE_VERSION`). Похідні (`APP_MUTEX`,
`SECRET_KEY`) обчислюються з них — не дублювати. `TEMPLATE_VERSION` — база шаблону для
оновлень ядра (див. розділ «Оновлення від шаблону»).

---

## Структура

```
PhonesStatus/
├── main.py                 ← crash_handler → lock → cleanup → updater_cleanup → config → color_theme → GUI
├── core/                   ← ОБОВ'ЯЗКОВЕ ЯДРО (не змінювати без причини)
│   ├── logger, crash_reporter, cleanup, config_manager, build_info, paths,
│   │   _build_dist (gen), _build_secrets (gitignored), updater, events,
│   │   instance_lock, elevation, dpapi (проєктне: шифрування секретів)
├── gui/                    ← app_window, widgets, hotkeys, context_menu, theme,
│                             update_dialog, window_utils, settings_window, progress
├── parsers/                ← ДОМЕН — джерела даних + порівняння:
│   │   table_rules (СПІЛЬНІ правила читання таблиці)
│   │   xlsx_parser (ручна таблиця)   sheets_parser (авто-таблиця, Sheets API)
│   │   pbx_manual (ручна вставка)    pbx_ari (авто-АТС)
│   │   comparator (один на всі режими)
├── integrations/           ← ДОМЕН — зовнішні системи ПОЗА звіркою (1.3.0):
│   │   mikrotik (DHCP-lease роутера: MAC → IP)
│   │   google_sheets (OAuth2 + запис у ту саму таблицю)
│   │   ip_sync (оркестрація двох сценаріїв для GUI)
├── formatters/             ← ДОМЕН: result_formatter (звіт), ip_list (список IP)
├── optional_modules/       ← опційне; копіювати у core/ або gui/ за потреби
├── assets/  → icons, images, themes/neutral.json, updater/
├── data/                   ← settings.json + вхідні файли
├── logs/                   ← app_YYYY-MM-DD.log (gitignored; тримаємо 30 днів)
└── build_portable.spec / requirements.txt
```

`core/` і `gui/` — основа шаблону, копіюються майже без змін. Логіка проєкту —
у `parsers/`, `integrations/` і `formatters/`.

⚠️ **Чим `integrations/` відрізняється від `parsers/`.** Не «новішим кодом», а
контрактом: модулі `parsers/` зобов'язані повернути `dict[номер, 'ON'|'OFF']` і
живлять `comparator`. В `integrations/` інший ключ (MAC), інший вихід (IP) й інша
задача — фізична адреса пристрою, а не стан реєстрації. Новий модуль кладеться сюди
лише тоді, коли він **не** бере участі у звірці; інакше його місце в `parsers/`.

---

## Доменні знання (не виводяться з коду з першого погляду)

### Контракт джерел даних — головний інваріант (з 1.2.0)

**Усі чотири джерела повертають ОДНЕ Й ТЕ САМЕ: `dict[номер, 'ON'|'OFF']`.**

| Режим | Модуль | Стан |
|---|---|---|
| Таблиця, ручний | `parsers/xlsx_parser.read_xlsx` | працює |
| Таблиця, авто | `parsers/sheets_parser.read_sheet` | працює (Google Sheets API + ключ) |
| АТС, ручний | `parsers/pbx_manual.read_manual` | працює |
| АТС, авто | `parsers/pbx_ari.read_ari` | працює (перевірено на бойовій АТС) |

Два перемикачі режиму **незалежні** → 4 комбінації. Саме завдяки спільному контракту
нижче по потоку (`compare` → `result_formatter`) розгалужень за режимом **немає взагалі**.
Додаючи джерело — тримай контракт, не чіпай comparator.

### Правила читання таблиці (`parsers/table_rules.py`)

⚠️ **Правила живуть тут, а не в парсерах.** Таблиця приходить із двох джерел
(`.xlsx` і Google Sheets), і обидва зобов'язані інтерпретувати її однаково. Доти
правила існували в одному екземплярі лише тому, що джерело було одне — з появою
другого копії розійшлися б на першій же правці. Не дублювати їх у парсери.

- Аркуш і колонки — **з конфігу**, не вшиті (`table_sheet`, `table_col_number`,
  `table_col_status`). Дефолти — `F` (номер) і `H` (статус), аркуш порожній = автовизначення.
  Винесені в налаштування, бо розкладка вже переїжджала: до 2026-08 номер був у **B**,
  потім B стала «Поверх». Наступний переїзд — правка в GUI, не в коді.
- Автовизначення аркуша: назва містить **«телефон»**, не містить `старий`/`copy of`/`експорт`;
  з кількох кандидатів береться **останній**. ⚠️ На робочому файлі воно **не спрацьовує** —
  єдиний аркуш зветься «Copy of Телефони 27.08.2026», тобто відпадає за виключенням, і рятує
  лише фолбек на активний аркуш. Тому явний вибір аркуша й з'явився.
- Номер нормалізується через `str(int(float(...)))` — щоб `1026.0` став `1026`. Приймаються
  лише **3–5 цифр** (`^\d{3,5}$`). ⚠️ Комірка має бути **чистим числом**: `901 (Binotel)` не
  розпізнається (зовнішня АТС Binotel — свідомо поза порівнянням).
- Статус приймається лише `ON`/`OFF` (після `.strip().upper()`); інше — рядок пропускається.

### Формат вставки з FreePBX (`parsers/pbx_manual.py`)

Приймає таблицю (`PJSIP  1001  0`) і голий список. Рядок ріжеться по пробілах, береться
**перший токен, що ЦІЛКОМ складається з 3–5 цифр**; решта ігнорується.

⚠️ Перевірений наслідок: номер, «вклеєний» у більший токен, **не** розпізнається —
`SIP/1005-00001` пропускається. Так само відкидаються `12` і `123456`. Підтримка формату
`SIP/xxxx-...` — свідома зміна регексу, а не «виправлення бага».

⚠️ **При дублі (номер в обох полях) перемагає OFF.** Правило живе тут, а не в comparator:
це артефакт саме ручного вводу — ARI дублів не дає взагалі.

### Формат Google Sheets (`parsers/sheets_parser.py`)

- **Sheets API v4 + API-ключ**, не анонімний `export?format=csv`. Анонімний запит
  Google має право зустріти HTML «unusual traffic», 429 або капчею — вибір формату
  цього не змінює. Ключ переводить запити в **квоту проєкту Cloud**.
- `valueRenderOption=UNFORMATTED_VALUE` — числа приходять числами, тож правило
  «чиста комірка» працює як із `.xlsx`. Без нього все стало б рядками.
- Діапазон запитується від `A` до дальшої з двох потрібних колонок — тоді індекс
  збігається з літерою.
- ⚠️ Google віддає **HTTP 400 і на протухлий ключ, і на криву назву аркуша**.
  Діагностика їх розрізняє за текстом помилки (перевірено живим прогоном) —
  інакше користувач шукає проблему не там.
- Ключ і посилання вводить користувач; ключ — у `SECRET_FIELDS` під DPAPI.

### Формат Asterisk ARI (`parsers/pbx_ari.py`)

- Один запит `GET {ari_url}/endpoints`, Basic-авторизація, лише `urllib`.
- Відповідь: `[{"technology":"PJSIP","resource":"1001","state":"online","channel_ids":[]}]`.
  Станів рівно два — `online`/`offline`.
- Трункові псевдо-endpoint'и (`SipProvider`, `To_Yeastar`) відсіюються **тим самим**
  правилом `^\d{3,5}$` — окремого списку виключень не треба, вони просто не числа.
- ⚠️ GraphQL/REST **самого FreePBX не годиться**: віддає лише статичну конфігурацію
  extension'а, стан реєстрації живе в Asterisk. Це відома обмеженість FreePBX.
- Помилки перекладаються в текст для користувача (`AriError`): 401 → логін/пароль,
  таймаут → bindaddr/фаєрвол, refused → mini-HTTP Server вимкнений.

### IP з MikroTik (`integrations/mikrotik.py`) — потік ПОЗА звіркою

Один запит `GET {url}/rest/ip/dhcp-server/lease`, Basic-авторизація, лише `urllib`.

⚠️ **REST живе на вебсервісі `www-ssl`, а не на `api`/`api-ssl`.** Попри назву, «API»
у MikroTik — окремий **бінарний** протокол на 8728/8729; він вимагав би pip-бібліотеки
або ~150 рядків саморобного протоколу. Ті сервіси лишаються вимкненими, а політика
`api` у групі користувача **зайва** — потрібні `read` і `rest-api`.

- **Адреса вводиться З ПОРТОМ.** У бойовій конфігурації `www-ssl` перевішено на **8443**,
  бо 443 був зайнятий і сервіс висів як `invalid`. Модуль сам додає `https://`, якщо
  схему не вказали.
- **Сертифікат самопідписаний за визначенням** (створюється на роутері), тож TLS завжди
  unverified. Прапорця в налаштуваннях свідомо немає — він удавав би вибір.
- ⚠️ Порт має бути дозволений у `chain=input` (`protocol=tcp`). Winbox (8291) може
  працювати, коли 8443 фільтрується — це збиває з пантелику: адреса правильна, а
  «нема доступу».

**Правило чотирьох випадків** (живе тут, не в GUI й не в `ip_sync`):

| Стан lease | Пишемо |
|---|---|
| `active-address` заповнене | `active-address` |
| порожнє, lease **статичний** | `address` (зарезервований IP) |
| порожнє, lease **динамічний** | `—` |
| MAC у lease не знайдено | `—` |

Порядок перевірки важливий: у статичного **активного** lease `address` і
`active-address` можуть розійтись, і правильна саме активна адреса.

⚠️⚠️ **RouterOS віддає НЕВАЛІДНИЙ JSON — тому `json.loads(..., strict=False)`.**
У полі `class-id` (DHCP Option 60, Vendor Class Identifier) трапляється **літеральний
нульовий байт**: значення надсилає сам клієнтський пристрій, а RouterOS кладе його
в JSON не екранувавши. RFC 8259 забороняє `U+0000..U+001F` усередині рядків, тож
strict-розбір валив увесь запит (424 КБ, 916 lease) через один байт в одному полі,
якого ми не читаємо. Уражених — **2 з 916**: на семплі це не проявиться. Не «спрощуй»
назад до `strict=True`.

Ще три факти з бойового прогону, кожен рятує від наївної реалізації:

- ⚠️ **`status` НЕ читаємо взагалі.** У статичного офлайн-lease він `"waiting"`, а не
  `"bound"` — перевірка на `bound` хибила б рівно на тому випадку, заради якого модуль
  і писався.
- **Усі значення приходять рядками** (`"dynamic": "false"`), ні булевих, ні чисел.
  Відсутній `dynamic` трактуємо як **динамічний**: записати чужу адресу гірше за прочерк.
- **Індексуємо за `mac-address`, не за `active-mac-address`** — другий це MAC клієнта,
  що зараз тримає адресу. Дублі MAC існують (кілька DHCP-серверів на роутері),
  перемагає останній, лічильник іде в лог.

⚠️ **Межа застосовності.** `active-address` — індикатор **прив'язки**, а не живості:
після зникнення пристрою lease лишається заповненим до кінця `lease-time`. Тут
`expires-after` ≈ 4 хвилини, тож вікно застарілості мізерне. Подовжать lease-time до
годин — правило почне брехати. ARP другим джерелом і пороги за `last-seen` свідомо
**не** додані: роутер уже відповів на це питання, а друге джерело породило б правила
пріоритету й невидимі у виводі гілки.

### Запис у Google-таблицю (`integrations/google_sheets.py`)

Читання авто-режиму йде на API-ключі, а ключ дає **тільки читання**. Запис потребує
OAuth2 → окремий модуль; `parsers/sheets_parser.py` не чіпається взагалі. Два шляхи
читання однієї таблиці з різною авторизацією — свідомий чистий шов.

- **OAuth2 loopback**: PKCE + `state`, `http.server` на випадковому порту `127.0.0.1`
  (не `0.0.0.0` — інакше питає Windows Firewall), браузер за умовчанням. Embedded-браузер
  виключено: важка залежність, і Google блокує такі клієнти (`disallowed_useragent`).
- `access_type=offline` + `prompt=consent` **обов'язкові разом** — без них refresh-токен
  не приходить.
- ⚠️ Три пастки, перевірені на живому API: токен іде заголовком **`Authorization: Bearer`**
  (не `?access_token=`); значення пише **`…/values:batchUpdate`** (а не `…:batchUpdate` —
  то структура книги); **`valueInputOption=RAW`** обов'язковий.
- ⚠️ **Пастка 7 днів.** Consent screen External у статусі *Testing* вбиває refresh-токен
  через тиждень. Застосунок переведено в **«In production»**. Верифікацію не проходимо
  свідомо — для неї потрібен власний домен, а вона лише прибирає екран «Google hasn't
  verified this app». `invalid_grant` — **штатний стан**, не аварія: GUI просить
  авторизуватись заново.
- `client_id`/`client_secret` беруться з **JSON від Google Cloud** (корінь `installed`)
  і лягають у конфіг під DPAPI. У збірку не вшиваються: вшитий у portable `.exe` secret
  дістається звичайним `strings`, а зміна клієнта вимагала б перезбірки.
- Акаунт, під яким пройшов OAuth, має бути **власником або редактором** таблиці.
  Публічний доступ «редактор» піднімати не треба.

### Оркестрація IP (`integrations/ip_sync.py`)

⚠️ **`write_ips` перечитує MAC-колонку безпосередньо перед записом і зіставляє за MAC.**
Номери рядків з моменту парсингу не використовуються — тому їх немає навіть у `Row`.
Між «Спарсити» і «Додати в таблицю» хтось може вставити чи видалити рядок, і за
кешованими позиціями IP тихо поїхали б у сусідні рядки, без жодної помилки. Один
зайвий read-запит прибирає цілий клас багів.

⚠️ **Колонка IP перевіряється строго й на колізію.** `table_rules.col_index` при кривій
літері мовчки бере дефолт — для читання це рятує, для ЗАПИСУ це спосіб залити IP у чужу
колонку робочої таблиці. Тому в `ip_sync` свій `_strict_column`, який відмовляється
працювати, плюс заборона збігу колонки IP з колонками номера, MAC і статусу.

Дубль MAC у таблиці → IP пишеться в **усі** його рядки (наслідок обходу рядків, не
окреме правило). Порожній чи сміттєвий MAC → рядок пропускається.

### Правило порівняння (`parsers/comparator.py`)

- `compare(table: dict, pbx: dict) -> Comparison` — симетрично, обидва боки однакові.
- **Змінами** вважаються лише номери в **обох** джерелах із різним статусом (правило
  не мінялось із 1.0.0). Сортування — за номером як числом.
- Додатково повертаються `only_table` і `only_pbx` — номери поза перетином.

### Формат звіту (`formatters/result_formatter.py`)

Кольорові сегменти `(текст, тег)` для GUI + плаский текст для `.txt`. Теги
(`dim`/`accent`/`green`/`red`/`yellow`/`bold`) — імена; кольори зіставляє
`app_window._apply_result_tags`. Ширина рамок — 52 символи.

⚠️ Секція **«у таблиці, немає в АТС» друкується лише при `pbx_complete=True`** (авто-режим).
У ручному режимі цей список дорівнював би «все, чого ви не вставили» — шум. Секція
«в АТС, немає в таблиці» показується завжди: вона мала й змістовна в обох режимах.

## Оновлення від шаблону

Проєкт побудовано на версії шаблону `core/build_info.py → TEMPLATE_VERSION` (**2.5.0**).
Щоб підтягти нові фічі/виправлення ядра з новішої версії шаблону:

1. Звір свою `TEMPLATE_VERSION` з останнім релізом шаблону.
2. Відкрий у **репо шаблону** `docs/HOW_UPDATE_YOUR_PROJECT.md` (цей файл у проєкт не їде) —
   Частина I містить процедуру, Частина II — таблиці файлів від твоєї версії до цільової.
3. Оновлюй **вибірково за статусами** (✅/⚠️/🔒/🆕) в окремій гілці, не наосліп — щоб не
   зруйнувати свої кастомізації. Після злиття — онови `TEMPLATE_VERSION`.

### Свідомі відхилення від skeleton (при оновленні НЕ затирати)

| Файл | Відхилення | Чому |
|---|---|---|
| `core/config_manager.py` | `remember = True` (шаблон: `False`) | режим «без слідів» стирав би `xlsx_path` і геометрію щоразу |
| `core/config_manager.py` | `update_check = "never"` | GitHub-релізів ще немає; вмикається одним рядком |
| `core/config_manager.py` | ключі `xlsx_path`, `window_geometry` | стан програми між сесіями |
| `gui/widgets.py` | `STATUS_COLORS` + проєктні `build_*` | палітра шаблону не описує акценти ON/OFF/попередження |
| `gui/app_window.py` | повністю проєктний | завжди кастомний файл (🔒 skip у таблицях оновлення) |
| немає `build_installed.spec` | обрано лише portable | installed-версія не планується; spec завжди можна взяти з шаблону |
| `gui/progress.py` | **виправлений дефект шаблону 2.5.0** | `except Exception as exc:` + `lambda: ...exc` — Python наприкінці блоку робить неявний `del exc`, лямбда виконується пізніше через `after` і падає `NameError`. Гілка `on_error` у `BackgroundTask` не працювала **ніколи**. Прив'язано значенням: `lambda e=exc:`. **При оновленні від шаблону НЕ затирати, доки дефект не виправлять upstream** |
| `core/dpapi.py` | немає в шаблоні взагалі | справжнє шифрування секретів (Windows DPAPI). У шаблоні лише специфікація — `docs/BACKLOG.md`, дефект **D-38**, статус ⬜. Код узято з проєкту TelegramSenderBot. `optional_modules/core/crypto.py` свідомо НЕ брали: він чесно називає себе обфускацією |
| `gui/settings_window.py` | шаблонний + 4 проєктні секції | ARI, розкладка таблиці, MikroTik, Google-авторизація — усі на `self._body` (`CTkScrollableFrame`), вимога 2.5.0, інакше опиняться поза прокруткою. Номери рядків `grid` жорсткі: додаючи секцію, зсувай наступні |
| `gui/widgets.py` | `height=110` у `build_input_panel`, `width` у текстових полях, `build_ip_panel` | виправлення розкладки й панель списку IP (1.3.0) |

> Не плутати із **системою оновлень продукту** (`updater.py`) — то авто-заміна `.exe` для
> кінцевого користувача, а не синхронізація коду з шаблоном.

### Успадковані дефекти ядра — ЗАКРИТІ в 2.5.0

Синхронізація 2.3.2 → 2.5.0 зняла чотири дефекти, які проєкт успадкував мовчки:

| Було | Стало |
|---|---|
| `assets_dir()` рахувався від `app_dir()` → у onefile-`.exe` тема й `updater.exe` не знаходились | `resource_dir()` (`sys._MEIPASS`); `assets_dir()` рахується від нього |
| `logger` робив `makedirs(logs_dir())` на імпорті → `.exe` у теці без прав не стартував узагалі | третій корінь `writable_root()` з фолбеком у `%LOCALAPPDATA%`/`%TEMP%`, переїзд гучний (`fallback_reason()`) |
| `neutral.json` мав `#48566280` (RGBA — формат, якого в Tk немає) → `TclError` на кожне наведення миші у **світлій** темі | `#485662` |
| `.gitignore`: `core/_build_dist.py  # коментар` — inline-коментар робив патерн неробочим, файл **не ігнорувався** | коментар окремим рядком; перевірено `git check-ignore -v` |

Ще два, які проявились би лише при релізі: апдейтер міг лишити без програми
(`sys.exit(1)` стояв перед гілкою рестарту), а `_find_asset` при невідомому
`DISTRIBUTION` вантажив перший-ліпший asset (`"...".endswith("")` завжди істинне).

---

## Стиль коду

- Коментарі й docstrings — **українська**; назви змінних/функцій/класів — **англійська**.
- Type hints для публічних функцій; docstrings для публічних функцій і класів.
- Логування — тільки через `from core.logger import log`. `try/except` з `log.error(...)`,
  ніколи мовчазний `pass`.
- Імпорти — абсолютні (`from core.logger import log`).

---

## Правила роботи

### Версія та git status

На початку сесії і перед будь-яким версійним рішенням — виконати `git status`:
- Порівняти `APP_VERSION` у `core/build_info.py` з останнім комітом / тегом.
- Якщо версія в файлах **новіша** за коміт → запитати: «накопичуємо далі в X.X.X
  чи створюємо нову версію?»
- Якщо версія **збігається** з комітом → запитати: «яка версія для нових змін?»
- **Ніколи не вирішувати номер версії самостійно** — тільки пропонувати і чекати підтвердження.

### Перед зміною коду
- Прочитай відповідний файл перед пропозицією змін.
- Не переписуй те, що працює, без явної причини.
- Зміна кількох файлів — запитай, чи робити все одразу.
- Показуй повний новий варіант функції/файлу; після змін — вкажи, що ще оновити (CHANGELOG тощо).

### Домен
- **Алгоритми `parsers/` не змінювати без явного запиту** — формати Excel і FreePBX описані
  вище й перевірені на реальних даних. Зміна регексів чи вибору аркуша — окреме рішення.
- Формат звіту живе в `formatters/result_formatter.py`, не в GUI. Нове представлення —
  ще одна функція там, а не `insert()` у вікні.

### Event Bus
- Нові міжмодульні комунікації — через `core.events`, не прямі імпорти.
- Імена подій — константи з класу `Events` (не «голі» рядки).
- Підписуй **методи** (`self._on_x`), не голі лямбди (WeakMethod збере їх одразу).
  `unsubscribe` у `destroy()` — бажано.

### Тема
- `ThemeManager` НЕ зберігає конфіг — лише `emit(Events.THEME_CHANGED)`. Персистентність і
  застосування палітри до вікна — у `app_window._on_theme_changed` / `_apply_palette`.
- Віджети головної області створюй через фабрики `make_*` у `widgets.py` (інваріант Gray).
- Акценти ON/OFF беруться з `widgets.STATUS_COLORS` за resolved-режимом теми, не хардкодяться
  у вікні (у світлій темі One Dark-кольори нечитабельні).

### Система оновлень
- Версія — тільки в `core/build_info.py` (`APP_VERSION`).
- `DISTRIBUTION` підміняється spec-файлами (через `core/_build_dist.py`), не вручну.
- `TEMP_PREFIX` узгоджений у `build_info.py` і `assets/updater/updater_src.py` (`phonesstatus_`).
- `GITHUB_TOKEN` живе в `core/_build_secrets.py` (**gitignored**), НЕ в `build_info.py`:
  токен, покладений у версійований файл, лишається в git-історії назавжди. Шаблон —
  `core/_build_secrets.py.example`. Лише fine-grained PAT `Contents:Read-only` на одному репо.
- Частота автоперевірки — ключ `update_check` (`startup`/`daily`/`weekly`/`never`),
  не булевий вимикач. Ручна перевірка — `AppWindow.check_updates_now()`.
- Перед першою portable-збіркою — зібрати `assets/updater/updater.exe` (див. нижче).
- При релізі: оновити `APP_VERSION` → зібрати → завантажити .exe на GitHub Releases.

### Що НЕ робити
- Не додавати зовнішні pip-залежності без обговорення.
- Не ускладнювати `core/` — він має лишатись мінімальним; проєктна логіка йде у
  `parsers/` / `formatters/`.
- Не пропонувати кросплатформенні рішення (Windows-only).
- Не змінювати `core/events.py` без вагомої причини.
- Не використовувати `grab_set()` у `UpdateDialog`.
- Не додавати розгалужень «якщо авто-режим» нижче за джерела даних. Увесь сенс
  спільного контракту в тому, що `comparator` і `result_formatter` про режими не знають.
  Єдиний свідомий виняток — прапорець `pbx_complete` у звіті, і він про **повноту даних**,
  а не про режим.
- Не проводити IP-потік через `comparator`/`result_formatter`. У нього інший ключ
  і інший вихід; спроба «уніфікувати» зруйнує головний інваріант проєкту.
- Не робити так, щоб галочка «збирати IP» писала в таблицю, і не зливати дві кнопки
  IP в одну «зроби все».
- Не винаходити центрування Toplevel і перевірку «вікно вже відкрите» — це
  `gui/window_utils.py` (`center_on_parent`, `get_instance`, `raise_window`,
  `clamp_to_screen`, `reset_entry`). Саме тому, що шаблон тричі винайшов їх
  незалежно — і всі три рази з тим самим дефектом.

---

## Відомі рішення — не чіпати без причини (стислий список)

- `_mutex` тримається у змінній в `main()` (GC не збирає handle).
- `threading.Thread + self.after(0, ...)` — єдиний безпечний місток у GUI-потік.
- `save_config` — tmp + fsync + `os.replace` (атомарний запис).
- `updater.exe` — окремий stdlib-only бінарник (Windows не дає перезаписати запущений .exe).
- ThemeManager НЕ пише конфіг (Варіант B); палітру до вікна застосовує підписник.
- Приватний asset — `asset["url"]` + octet-stream + ручний 302-редірект.
- `AppContextMenu` тримає одне `tk.Menu` (інакше кожен ПКМ-клік накопичує віджет).
- Усі джерела даних повертають `dict[номер, 'ON'|'OFF']` — завдяки цьому `comparator`
  і `result_formatter` не знають про режими. Не додавати туди `if auto`.
- Правило «при дублі перемагає OFF» живе в `pbx_manual.py`, а не в `comparator`:
  це артефакт ручного вводу, ARI дублів не дає.
- Секція звіту «у таблиці, немає в АТС» друкується лише при `pbx_complete=True`:
  у ручному режимі це був би список «усе, чого ви не вставили».
- Тіла панелей-джерел створюються ОДИН раз, перемикання — `grid`/`grid_remove`
  (перестворення віджетів губило б уміст полів вставки).
- Пароль ARI: відкритий у пам'яті, `dpapi:<base64>` на диску. `_encrypt_secrets`
  повертає КОПІЮ — інакше наступний читач ключа дістав би токен замість пароля.
  `SECRET_FIELDS` — шість полів (з 1.3.0), застереження стає тим важливішим.
- `json.loads(..., strict=False)` у `mikrotik.py` — не спрощувати назад: RouterOS
  віддає невалідний JSON (нульовий байт у `class-id`).
- Галочка «збирати IP» при звірці **лише парсить, у таблицю не пише ніколи**. Запис —
  тільки явним кліком із підтвердженням: оператор має знати, коли змінюється спільний
  документ. Стан галочки ефемерний, у конфіг не зберігається.
- Список IP ефемерний: живе в пам'яті `AppWindow` до закриття. Записана адреса
  протухає за хвилини, тож «вчорашній список» вводив би в оману сильніше за порожнє поле.
- Збої IP-потоку **ізольовані**: помилка MikroTik не гасить звіт порівняння — звірка
  показується завжди, помилка йде окремим повідомленням.
- `CTkTextbox` у `build_input_panel` має `height=110`. Без нього дефолтні 200px × два
  поля виштовхували панель результату за межі вікна в режимі «ручний+ручний».
- Колонки вікна мають `uniform="sources"` — інакше панелі джерел розходяться на
  26-54px залежно від режиму. Пропорція 60/40 для звіту й IP живе у **власному
  контейнері** рядка результату, щоб не зачепити панелі джерел.
- Базова ширина `CTkTextbox` (560 для звіту, 370 для IP) — не косметика: `grid`
  ділить за вагами лише надлишок понад природний розмір, тож без бази ваги 3:2
  дали б майже 50/50.

---

## Додавання нових модулів

| Тип модуля | Місце |
|------------|-------|
| Вікно або UI-компонент | `gui/` |
| Універсальна інфра-логіка (лог/конфіг/шина — рівень шаблону) | `core/` (рідко; зазвичай вже є) |
| Джерело даних для ЗВІРКИ (контракт `dict[номер,'ON'/'OFF']`) | `parsers/` |
| Зовнішня система ПОЗА звіркою (інший ключ/вихід) | `integrations/` |
| Проєктна не-UI логіка / алгоритм | `parsers/`, `formatters/` або новий `<domain>/` — **не** `core/` |
| Може знадобитись в інших проєктах | `optional_modules/` або промоція в шаблон |

Чекліст нового модуля: `snake_case.py` · type hints + docstring (укр.) · логування через
`core.logger` · комунікація через Event Bus · підписка методом класу · `unsubscribe` у
`destroy()` · нова подія — константа в `core/events.py` · нова залежність — у `requirements.txt`.

---

## Опційні модулі

`optional_modules/` містить готові модулі (crypto, health, state_manager, env_loader,
tray, dialogs, progress, splash). Підключити = скопіювати файл у `core/` чи `gui/` +
додати залежності + інтегрувати за сніпетом (`optional_modules/README_OPTIONAL_MODULES.md`).

**Підключені в 1.2.0** (скопійовані в `gui/`, з `optional_modules/` НЕ видалені —
там лишається еталон для звірки при оновленні шаблону):

| Модуль | Навіщо | Стан копії |
|---|---|---|
| `gui/progress.py` | `BackgroundTask` — читання `.xlsx` і мережа ARI поза GUI-потоком | ⚠️ **виправлений дефект**, див. таблицю відхилень |
| `gui/settings_window.py` | вікно налаштувань + дві проєктні секції (ARI, розкладка таблиці) | розширена |

`core/dpapi.py` в `optional_modules/` **немає взагалі** — його джерело поза шаблоном
(див. таблицю відхилень).

---

## Git і реліз

Робочий процес: перед комітом — `git status`, показ змін, **автоматична пропозиція labels**
на основі префіксу, очікування затвердження. Коміти та пуш виконуються з дозволу
користувача — в Claude Code (Bash) або вручну у VS Code.

**Префікси комітів та labels:** `init:` · `feat:` · `fix:` · `ui:` · `refactor:` ·
`config:` · `docs:` · `build:`
**Labels за шаром:** `layer: core` (🔴) · `layer: gui` (🟡) · `layer: docs` (🟢)
**Особливі** (за потреби): `breaking-change`, `backlog`.

Реліз: оновити `APP_VERSION`, annotated-тег **без префіксу** `X.Y.Z` (не `vX.Y.Z` —
збігається з `APP_VERSION` буква-в-букву; `updater.py` читає `tag_name` з GitHub Releases),
push тегів у обидва remote (`--tags`), зібрати й завантажити `*_portable.exe` на GitHub Releases.

**Подвійний push** — кожен push двома командами:
```
git push origin main    ← GitHub
git push backup main    ← локальний bare-репозиторій у ~ BACKUP ~
```

---

## ⚙️ Старт проєкту — збірка updater.exe (виконати ОДИН раз)

Потрібно **перед першою portable-збіркою** (`build_portable.spec` вкладає цей файл у `.exe`):

```bash
cd assets/updater
pyinstaller --onefile --noconsole --name updater updater_src.py
cp dist/updater.exe updater.exe
rm -rf dist build __pycache__ updater.spec
```

Після цього **не чіпай** ні `updater_src.py`, ні `updater.exe` — вважай обидва
замороженими. Перезбирати лише якщо свідомо змінюється логіка апдейтера.
`updater.exe` версіонується у Git проєкту (у `.gitignore` ігноруються лише
`assets/updater/build/`, `dist/`, `updater.spec`).
