"""
integrations/ — зовнішні системи, які НЕ беруть участі у звірці статусів.

Відрізняється від `parsers/` контрактом: модулі `parsers/` зобов'язані повертати
`dict[номер, 'ON'|'OFF']` і живлять `comparator`. Тут інший ключ (MAC замість
внутрішнього номера), інший вихід (IP замість статусу) й інша задача — фізична
адреса пристрою, а не стан реєстрації. Тому цей потік не проходить через
`comparator`/`result_formatter` і не порушує головний інваріант проєкту.

Склад:
  mikrotik.py       — DHCP-lease з роутера: MAC → IP
  google_sheets.py  — OAuth2 + читання/запис тієї самої таблиці
  ip_sync.py        — оркестрація двох сценаріїв для GUI
"""
